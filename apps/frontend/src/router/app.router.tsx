import { RouteObject } from "react-router-dom";

export const router: RouteObject = {
    path: "/",
    element: <></>,
    children: [
        {
            path: "/join",
            element: <></>
        }
    ]
}