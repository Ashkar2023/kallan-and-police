{
    "name": "backend",
    "version": "1.0.0",
    "main": "dist/index.js",
    "type": "module",
    "scripts": {
        "build": "rimraf dist && tsc && tsc-alias",
        "dev": "npm run build && set NODE_ENV=development && nodemon --env-file=.env dist/index.js",
        "start": "set NODE_ENV=production &&  node --env-file=.env dist/index.js"
    },
    "keywords": [],
    "author": "",
    "description": "",
    "dependencies": {
        "common": "workspace:*",
        "pino": "^9.7.0",
        "socket.io": "^4.8.1"
    },
    "devDependencies": {
        "@types/node": "^22.10.7",
        "nodemon": "^3.1.9",
        "rimraf": "^6.0.1",
        "tsc-alias": "^1.8.10",
        "typescript": "^5.7.3"
    }
}